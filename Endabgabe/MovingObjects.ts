namespace Endabgabe {

    export class MovingObject {
        xP: number;
        yP: number;
        xD: number;
        yD: number;
        colour: string;
        color: string;
        movedown: boolean;
        hitRodler: boolean;

        constructor() {/* vorher definiert */}
        draw(): void {/* vorher definiert */}
        moving(): void {/* vorher definiert */}
        driveDown(): void {/* vorher definiert */}
        driveUp(): void {/* vorher definiert */}
    } // Ende export class
} // Ende namespace